# === Database (PostgreSQL via asyncpg) ===
# Format: postgresql+asyncpg://<user>:<pass>@<host>:<port>/<dbname>
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/contacts_db

# === JWT Settings ===
# Secret key used to sign JWT tokens (use a long, random string)
SECRET_KEY=
# JWT signing algorithm
ALGORITHM=HS256
# Access token lifetime in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30
# Email verification token lifetime in hours
VERIFY_TOKEN_EXPIRE_HOURS=24

# === Redis (Rate Limiter & Cache) ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# === SMTP (Email) ===
# Credentials and server info for sending emails
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SERVER=
MAIL_PORT=

# === Admin User (Created Automatically on Startup) ===
ADMIN_EMAIL=
ADMIN_PASSWORD=

# === Frontend URLs for Email Links ===s
# (currently pointing to backend endpoints; replace with your actual frontend URLs)
FRONTEND_VERIFY_URL=http://localhost:8000/api/auth/verify-email
FRONTEND_RESET_URL=http://localhost:8000/api/auth/reset-password

# === Cloudinary (Avatar Uploads) ===
CLOUDINARY_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
